# Append-only Rules

A progress update is not a plan update.

The only permitted edit to `project_plan.md` is adding a dated progress log entry at the end or under the existing `# Progress Update Log` section.

If existing text must be changed, stop and route the user to the change/replan workflow.
