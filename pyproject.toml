[project]
name = "pm-plugin"
version = "0.1.0"
description = "PM plugin Python tooling — Gantt, Excel, and slide deck generation"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "openpyxl>=3.1.5",
    "pytest>=9.1.0",
    "python-pptx>=1.0.2",
]
